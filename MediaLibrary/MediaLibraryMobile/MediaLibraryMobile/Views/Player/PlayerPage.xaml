<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MediaLibraryMobile.Views.PlayerPage"
             xmlns:behaviors="clr-namespace:MediaLibraryMobile.Behaviors"
             Title="Player">
    <Grid>
        <MediaElement Source="{Binding Source}" ShowsPlaybackControls="{Binding ShowPlaybackControls}">
            <MediaElement.Behaviors>
                <behaviors:EventToCommandBehavior EventName="MediaEnded" Command="{Binding MediaEndedCommand}" />
            </MediaElement.Behaviors>
        </MediaElement>
        <Grid.GestureRecognizers>
            <SwipeGestureRecognizer Direction="Left" Command="{Binding Path=BindingContext.NextCommand, Source={RelativeSource AncestorType={x:Type ContentPage}}}" />
            <SwipeGestureRecognizer Direction="Right" Command="{Binding Path=BindingContext.PreviousCommand, Source={RelativeSource AncestorType={x:Type ContentPage}}}" />
        </Grid.GestureRecognizers>
    </Grid>
</ContentPage>